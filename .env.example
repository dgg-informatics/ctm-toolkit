# Copy to .env and fill in. Required by the LLM-assisted curation steps:
#   ctm-ctml
#   ctm-mm trials-curate
#   ctm-mm trials-confidence-split --recover-diagnosis
# Install those extras with: uv pip install 'ctm-toolkit[llm]'

# UMGPT (U-M's OpenAI-compatible gateway). Both are required — the CLIs fail
# fast with a clear error if either is missing.
UMGPT_API_KEY=
UMGPT_BASE_URL=

# Optional; defaults to gpt-4o when unset.
UMGPT_MODEL=gpt-4o

# Optional; where ctm-llm keeps its response cache. When unset the location is
# /var/lib/ctm/cache if that directory exists (the shared server cache), else
# ~/.cache/ctm (per-user). Set it to force one location — e.g. a group-writable
# shared dir so several accounts reuse one warm cache instead of re-paying for
# the same LLM calls.
# CTM_CACHE_DIR=/var/lib/ctm/cache


# ---------------------------------------------------------------------------
# MongoDB. Required by the steps that store their output in Mongo:
#   ctm-mm trials-diff
# Install with: uv pip install 'ctm-toolkit[db]'
#
# Note this is CTM's OWN MongoDB connection. It is NOT the same as
# MatchMiner's credentials, which matchengine still reads from
# SECRETS_JSON.json at the repo root (gitignored) — see the README's
# "MatchMiner Preparation and Running" section.
# ---------------------------------------------------------------------------

# All three required; the CLI fails fast naming whichever is missing.
MONGO_HOST=localhost
MONGO_PORT=27018

# This run's database. One database per run gives structural isolation, so this
# changes every run — or pass --db NAME to override it without editing .env.
MONGO_DBNAME=2026-08-17_dev

# The master trial list's database. Deliberately NOT per-run: the master is a
# rolling current-state artifact, so it has a fixed address. Reading it out of
# MONGO_DBNAME would query a database that is empty at the start of every run.
# No default — required only when trials-diff is run without --master.
MONGO_MASTER_DBNAME=ctm_master

# Optional; defaults to 06_master_trials when unset.
MONGO_MASTER_COLLECTION=06_master_trials

# The patient database that `ctm-mm load` ingests into. Fixed (not per-run): each
# load writes an immutable dated snapshot (<date>_clinical/genomic/patient_data)
# plus refreshed latest_* collections. Required by `ctm-mm load` unless --patient-db
# is passed.
MONGO_PATIENT_DBNAME=patients_dev


# ---------------------------------------------------------------------------
# Default file exports. In v2 the stages write only to MongoDB by default; two
# stages additionally drop a dated JSON into a canonical directory (override the
# path per-run with --out, or suppress with --no-disk).
# ---------------------------------------------------------------------------

# `ctm-llm biomarkers` → <dir>/<YYYY-MM-DD>_llm-biomarkers_trials.json
# Optional; defaults to /var/lib/ctm/to-curate when unset.
LLM_BIOMARKER_EXPORT_DIR=/var/lib/ctm/to-curate

# `ctm-mm trials-merge` → <dir>/trials_master-<YYYY-MM-DD>.json
# Optional; defaults to /var/lib/ctm/trials when unset.
MASTER_TRIAL_EXPORT_DIR=/var/lib/ctm/trials


# ---------------------------------------------------------------------------
# DDOTS — Sparrow's protocol registry, the trial list behind `--fetch-ddots`.
# Required only when fetching; `--ddots <dump.json>` needs neither.
#
# Note DDOTS takes credentials as QUERY PARAMETERS, so the secret ends up in
# any URL that gets logged. ddots_to_raw never prints the request URL; keep it
# that way, and avoid pasting a full URL into a terminal or ticket.
# ---------------------------------------------------------------------------
DDOTS_API_KEY=
DDOTS_SECRET_KEY=

# Optional; defaults to https://www.ddotscredit.com/rest/protocol/get
DDOTS_BASE_URL=

# Optional; scopes queries to one institution. Defaults to 18 (Sparrow). The
# DDOTS registry is shared across institutions, so an unscoped query returns
# other hospitals' protocols.
DDOTS_HOSPITAL_ID=18
